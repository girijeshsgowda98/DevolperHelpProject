@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model FilterListModel

@{
    Layout = "_Layout"; // If you have a layout
}
<style>
    table {
        border-collapse: collapse;
        width: 100%;
    }

    th, td {
        padding: 8px;
        text-align: left;
        border-bottom: 1px solid #DDD;
    }

    tr:hover {
        background-color: #D6EEEE;
    }
</style>
@*<form action="@Url.Action("Index", "RequestResp")" method="get" class="d-flex">
    <i class="bi bi-search"></i>
    <input class="form-control me-2" type="text" name="query" placeholder="Search" aria-label="Search">
    <button type="submit" class="btn btn-default">Search</button>
</form>*@

<partial name="_filterLogic" />
@if (Model.RequestDataModel != null)
{
    <h4>Api List</h4>

    <table class="content-table">
        <thead>
            <tr>
                <th>ModuleName</th>
                <th>Controler</th>
                <th>Action</th>
                <th>RequestTime</th>
                <th>ResponseTime</th>
                <th>UniqueId</th>
                <th>UserMasterId</th>
                <th>ClientCode</th>
                <th>UCCid</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var @group in Model.RequestDataModel)
            {
                <tr>
                    <td>@group.moduleName</td>
                    <td>@group.controlName</td>
                    <td>@group.actionName</td>
                    <td>@group.requestedon</td>
                    <td>@group.responseon</td>
                    @*<td>@group.uniqueid</td>
                    <td>@group.usermasterid</td>
                    <td>@group.cliendcode</td>
                    <td>@group.uccid</td>*@
                </tr>
            }
        </tbody>
    </table>
    }
</br>
@if(Model.ExceptionsDataModel != null){
    <h4 >Exception List</h4>

    <table class="content-table">
        <thead>
            <tr>
                <th>ModuleName</th>
                <th>Controler</th>
                <th>Action</th>
                <th>LastCreatedOn</th>
                <th>Uniqueid</th>
                <th>Cliendid</th>
                <th>uccid</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var @group in Model.ExceptionsDataModel)
            {
                <tr>
                    <td>@group.moduleName</td>
                    <td>@group.controlName</td>
                    <td>@group.actionName</td>
                    <td>@group.createdon</td>
                    <td>@group.uniqueid</td>
                    <td>@group.cliendcode</td>
                    <td>@group.uccid</td>
                </tr>
            }
        </tbody>
    </table>
    }
    <script>
        var inputField = document.getElementById("pop");   
        inputField.value  = "Exceptions"
        
    </script>
