@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model RequestModel

@{
    Layout = "_Layout"; // If you have a layout
}
<partial name="_filterLogic" />
@*@using (Html.BeginForm("Index", "Filter", FormMethod.Get))
{
    <div class="input-container">
        <input type="hidden" name="type" value="Apis">
        <input type="text" name="modulename" placeholder="ModuleName">
        <input type="text" name="control" placeholder="Control">
        <input type="text" name="input" placeholder="Input">
        <input type="submit" value="Filter">
    </div>
}*@

<h4 class="head-font">Api List</h4>

<table id="tableData" class="content-table">
    <thead>
        <tr>
            <th>ModuleName</th>
            <th>Controler</th>
            <th>Action</th>
            <th>RequestTime</th>
            <th>ResponseTime</th>
            <th>TotalRequest</th>
            <th>AverageTime</th>
        </tr>
    </thead>
    </table>
    @*<tbody>
        @foreach (var @group in Model.RequestDataModel )
        {
            <tr>
                <td>@group.moduleName</td>
                <td>@group.controlName</td>
                <td>@group.actionName</td>
                <td>@group.requestedon</td>
                <td>@group.responseon</td>
                <td>@group.TotalRequest</td>
                <td>@group.AverageTime</td>
                
            </tr>
        }
    </tbody>
</table>
</br>
<h4>Request List Top 5 Most</h4>

<table class="content-table">
    <thead>
        <tr>
            <th>ModuleName</th>
            <th>Controler</th>
            <th>Action</th>
            <th>RequestTime</th>
            <th>ResponseTime</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var @group in Model.RequestListModel)
        {
            <tr>
                <td>@group.moduleName</td>
                <td>@group.controlName</td>
                <td>@group.actionName</td>
                <td>@group.requestedon</td>
                <td>@group.responseon</td>
            </tr>
        }
    </tbody>
</table>*@
@section Scripts{
    <script src="~/js/RequestResp.js"></script>
}
